/ locals: command, current_server, refreshed_territory, refreshed_player, retry_territory
- if command.settled?
  = render "servers/territories/command_settled", command:, target: service_command_id(command), current_server:, refreshed_territory:, refreshed_player:, retry_territory:
