/ locals: command, dom_id, current_server, refreshed_player, viewing_self
- if command.settled?
  = render "servers/players/reset_settled", command:, target: dom_id, current_server:, refreshed_player:, viewing_self:
- else
  = turbo_stream.replace dom_id do
    = render "servers/players/reset_processing", command:
