/ Response to DELETE /servers/:server_id/favorite. Swap the toggle back to its
/ not-favorited state in place.
/ locals: current_server
= turbo_stream.replace "favorite_button_#{current_server.public_id}" do
  = render "servers/favorites/button", server: current_server, favorited: false
