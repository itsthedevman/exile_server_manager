/ locals: current_server
#reward_code_section
  - if !current_server.connected?
    .small.text-muted
      i.bi.bi-plug.me-1
      | #{current_server.server_id} is offline. Rewards are handed over in game, so a code has to wait until it is back.
  - else
    .small.text-muted.mb-2 Been given a reward code?

    = form_with url: lookup_server_reward_path(current_server.public_id), method: :post
      .input-group.input-group-sm
        = text_field_tag :reward_id, nil, class: "form-control", placeholder: "Reward code", autocomplete: "off", required: true, aria: {label: "Reward code"}
        = submit_tag "Add", name: nil, class: "btn btn-outline-success"

    = render "servers/rewards/code_message", message: nil
