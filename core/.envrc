use flake ..

# Core no longer ships its own Gemfile; reuse service's bundle so
# `bundle exec rspec` from this directory hits service's installed gems.
export BUNDLE_GEMFILE="$ESM_SERVICE_PATH/Gemfile"
