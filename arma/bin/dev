#!/usr/bin/env bash

set -euo pipefail

readonly args=("$@")

# Tests live in bin/test, not the dev lifecycle: run them when you want them.
exec "./bin/build" --target=linux --start-server "${args[@]}"
